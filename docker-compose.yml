services:
  bot:
    build: .
    container_name: qqbot
    restart: always
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - ./config.json:/bot/config.json
      - ./插件:/bot/插件
      - ./数据:/bot/数据
      - ./日志:/bot/日志
